# Outputs

Generated experiment figures are grouped by experiment type.

```text
outputs/
├── zoning/   # Current `zone_footprint()` showcase figures.
└── layout/   # Future room-group + hub/corridor experiment figures.
```

The checked-in PNGs under `zoning/` are current reference outputs for the
33-case zoning showcase.
