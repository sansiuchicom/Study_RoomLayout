"""proto3.data — package-internal data files (non-Python content).

Houses domain rules (`target_rules/*.json`) and any future static reference
data shipped with proto3. Distinct from `src/proto3/` Python modules to
preserve the engine ↔ data separation (Plan S06-D4, S06-D17).
"""
