"""proto3.data.target_rules — per-typology default domain rules (JSON).

Apartment defaults populated in Plan §4.3 (S06-D2, D3, D4).
External callers override via `TargetAdapter(rules_path=...)` (S06-D5, D17).
See README.md for the 3-layer extensibility model + mission scope.
"""
