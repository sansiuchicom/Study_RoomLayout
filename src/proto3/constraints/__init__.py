"""proto3.constraints — domain feasibility gates as pure functions (Step 06).

`gates` module exposes pure-function gate predicates used by
`proto3.stages.stage02_gate` and (per Plan Def-5) future Stage 11/13
binding in Step 12.

See 006_Step06_ProgramConstraintEngine_Plan.md §3 / S06-D13.
"""
