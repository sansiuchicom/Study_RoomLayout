"""proto3.data.target_rules — per-Target default domain rules (JSON).

Apartment defaults populated in Plan §4.3 (S06-D2, D3, D4).
External callers override via `ApartmentAdapter(rules_path=...)` (S06-D5, D17).
"""
