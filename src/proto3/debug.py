"""DebugArtifact + debug output folder contract (S02-D5).

Defines the canonical 17 filename constants for outputs/debug_runs/<run_id>/
(Pipeline Overview §12.2) plus run_folder() and stage_svg_filename() helpers.
Write functions are deferred to Step 03 (visualization).

Populated in P5 #3 (4.3).
"""
