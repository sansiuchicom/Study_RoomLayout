"""High-level decomposition wrapper — Step 05 §4.x will populate.

Will host: auto_partition (one-line entry point) and the proto3 schema
adapter mapping algorithm output → GeometricPiece + Atom.
"""


def auto_partition(*args, **kwargs):
    raise NotImplementedError(
        "auto_partition() — Step 05 §4.x; see references/lir_recursive_per_family_v3_2.py"
    )
