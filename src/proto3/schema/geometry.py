"""Geometry schemas: GeometricPiece, Decomposition (S05-Dxx, D019).

Output of v3.2 algorithm (`proto3.geometry.decompose.auto_partition`).
Distinct from D006 Region/RegionSet — Region is the architectural territory
layer (lobe/bay/public-candidate/private-candidate) populated in Step 07/09;
GeometricPiece is the algorithm-derived geometric subdivision.

Step 05 §4.x will populate this module.
"""
