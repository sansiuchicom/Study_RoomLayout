"""``build_region_graph(regions)`` — Phase 4 graph.

Placeholder. Populated in work item 4.10.

Plan reference: ``003_Step03_GeometryPipeline_Plan.md`` §4.10.

Will define ``build_region_graph(regions)`` returning a ``networkx``
graph of region adjacencies. Edges represent shared boundary segments
between regions. Consumed by Step 04 growth seeding.

Bundled work item (Plan §4.10): region-graph viz overlay (into
``viz/stages/regionize.py``) and 33-case ``region_graph.json`` + PNG
golden fixtures land in the same commit as this module.
"""
