"""Territory dataclass + ``resolve_territories(atoms)`` — Phase 4 prep.

Placeholder. Populated in work item 4.8.

Plan reference: ``003_Step03_GeometryPipeline_Plan.md`` §4.8.

Will define:

- ``@dataclass(frozen=True) Territory`` — atom cluster intermediate
  used by ``regionize``.
- ``resolve_territories(atoms, ...)`` — group atoms into territories.

Intermediate stage: **no standalone viz** (folded into regionize
rendering) and **no standalone golden** (output absorbed into the
``regionize.json`` golden per case).
"""
