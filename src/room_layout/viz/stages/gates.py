"""Gate visualization — ``save_gate_figure`` + dimension diagnostics.

Placeholder. Populated in work item 4.11.

Plan reference: ``003_Step03_GeometryPipeline_Plan.md`` §4.11.

Will define:

- ``save_gate_figure(shape, regions, gate_results, path)`` — regions
  colored by pass/fail with failure annotations (``code`` + short
  message).
- ``save_dimension_examples_figure(policy, path)`` — singleton
  diagnostic illustrating ``DimensionPolicy`` parameters (Cell
  ``viz.py::save_dimension_examples_figure``).
"""
