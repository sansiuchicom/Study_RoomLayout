"""``save_input_figure(shape, path)`` — render ShapeInput parts.

Placeholder. Populated alongside the first stage that needs it
(work item 4.7).

Plan reference: ``003_Step03_GeometryPipeline_Plan.md`` §4.7.

Will render a ``ShapeInput``'s parts as colored polygons with
``part_id`` + ``theta`` labels and vertex dots. Visual vocabulary
reference: Cell ``viz.py::save_input_figure``.
"""
