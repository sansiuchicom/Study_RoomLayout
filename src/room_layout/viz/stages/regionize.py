"""Regionize visualization — ``save_region_figure`` + region-graph overlay.

Placeholder. Populated in work items 4.9 (regions) and 4.10 (graph).

Plan reference: ``003_Step03_GeometryPipeline_Plan.md`` §4.9 + §4.10.

Will define:

- ``save_region_figure(shape, atoms, regions, path)`` — atom backdrop
  (faint grey) + region color overlay with ``R#`` / area-in-m²
  labels.
- ``save_region_graph_figure(shape, regions, graph, path)`` —
  region-graph nodes + adjacency edges overlaid on regions.

Visual vocabulary reference: Cell ``viz.py::save_region_figure`` +
``save_region_graph_figure``.
"""
